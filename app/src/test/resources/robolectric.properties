# The manifest/resources/assets are provided by AGP via includeAndroidResources,
# so no manifest override is needed here (it broke under Robolectric 4.10+).
# Pin the emulated SDK so tests run against a fixed API level.
sdk=29
